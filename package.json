{
  "name": "yawningface",
  "version": "0.1.0",
  "private": true,
  "description": "Open-source, free, cross-device distraction blocker - one blocking brain on every device.",
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "apps/cli",
    "apps/extension"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20"
  }
}
